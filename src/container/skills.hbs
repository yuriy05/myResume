{{!--
Це код container файлу для skills сторінки,
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/skills"
--}}

<div>
    {{#with main}}
    <main>
        <section>
            {{> title data="All skills" big=true}}
            <ul>
                {{#each skills}}
                <li>
                {{name}}
                {{#if lvl includeZero=true}}({{lvl}}){{/if}}
                {{#unless lvl includeZero=true}} (не вивчаю){{/unless}}
                {{#unless lvl}}
                {{#if lvl includeZero=true}}(Ще вивчаю){{/if}}
                {{/unless}}
                </li>
                {{/each}}
            </ul>
        </section>
        <br />
        <section>
            {{> title data="My hobbies"}}
            <ul>
                {{#each hobbies}}
                    <li>
                        {{name}}
                        {{#if isMain}}(favourite){{/if}}
                    </li>
                {{/each}}
            </ul>
        </section>
    </main>
    {{/with}}
</div>

{{!--
Цей код містить лише те, що буде виводитись в {{{body}}} в layout/default.hbs
Адже наш layout/default.hbs має вже структуру HTML
--}}